{{ config(materialized='table') }}

SELECT tdate,
  FORMAT('%3.2f',
    AVG(tips)) AS average_tip,
  FORMAT('%3.2f',
    MAX(tips)) AS max_tip
FROM
   dbt_taxi_trips.taxi_trips_summary
group by tdate
ORDER BY
  average_tip DESC
